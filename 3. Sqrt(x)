class Solution:
    def mySqrt(self, x: int) -> int:
        for i in range(x+1):
            if x==0:
                return 0
            else:
                if i*i==x:
                    return i
                elif i*i>x:
                    return i-1
            
            
